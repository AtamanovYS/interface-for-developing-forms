```bsl
// Функция экранирует кавычки для формата .csv 
//  
Функция ОтформатироватьЗначениеCSV(Значение, Разделитель)
	Значение = СтрЗаменить(Значение, """", """""");
	Если Найти(Значение, Разделитель) <> 0 Тогда
		Значение = """" + Значение + """";	
	КонецЕсли;
	Возврат Значение;
КонецФункции

```
